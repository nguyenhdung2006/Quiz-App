spring:
  config:
    import:
      - optional:file:./.env[.properties]
      - optional:file:../.env[.properties]
      - optional:file:./config/oauth2-google.yml
  profiles:
    group:
      production: prod
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            scope:
              - openid
              - profile
              - email
        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub

server:
  servlet:
    session:
      cookie:
        same-site: ${SESSION_COOKIE_SAME_SITE:lax}
        secure: ${SESSION_COOKIE_SECURE:false}
        path: ${SESSION_COOKIE_PATH:/}

app:
  frontend:
    base-url: ${FRONTEND_URL:http://localhost:5500/frontend}
    origin: ${CORS_ALLOWED_ORIGINS:${FRONTEND_URL:http://127.0.0.1:5500,http://localhost:5500}}
  oauth2:
    success-redirect-uri: ${OAUTH_SUCCESS_REDIRECT_URI:${app.frontend.base-url}/index.html}
  security:
    hsts:
      enabled: ${APP_SECURITY_HSTS_ENABLED:${SESSION_COOKIE_SECURE:false}}

ai:
  model: ${AI_MODEL:gpt-4.1-mini}
  openai:
    api-key: ${OPENAI_API_KEY:}
    responses-url: https://api.openai.com/v1/responses
  rate-limit:
    mode: ${RATE_LIMIT_MODE:in-memory}
    minute-window: ${AI_RATE_LIMIT_MINUTE_WINDOW:60s}
    explain:
      per-minute: ${AI_EXPLAIN_RATE_LIMIT_PER_MINUTE:10}
      per-day: ${AI_EXPLAIN_RATE_LIMIT_PER_DAY:100}
    deck:
      per-minute: ${AI_DECK_RATE_LIMIT_PER_MINUTE:3}
      per-day: ${AI_DECK_RATE_LIMIT_PER_DAY:20}
